<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- APIs backend : HTTPS obligatoire -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">skyplayerapp.xyz</domain>
        <domain includeSubdomains="true">firebaseio.com</domain>
        <domain includeSubdomains="true">googleapis.com</domain>
        <domain includeSubdomains="true">google.com</domain>
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </domain-config>

    <!-- Flux IPTV : HTTP autorisé (la plupart des serveurs providers n'ont pas de HTTPS) -->
    <!-- Le backend Sky-player (skyplayerapp.xyz) est déjà en HTTPS obligatoire ci-dessus -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
