# Sky Player Pro - Backend Configuration

# Clé API de communication sécurisée avec l'app Android
# Générer avec: openssl rand -hex 32
ACTIVATION_API_KEY=CHANGE_ME

# Firebase (optionnel si firebase-admin utilise les credentials par défaut)
# FIREBASE_SERVICE_ACCOUNT={"type":"service_account",...}

# Gemini AI (optionnel)
# GEMINI_API_KEY=your_key_here
